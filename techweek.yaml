Parameters:
  Env:
    Type: String
    AllowedValues: ['dev', 'test', 'prod']
    Default: 'dev'

Type: AWS::CloudFront::Distribution
Properties: 
  DistributionConfig: 
    DistributionConfig
  Tags: 
    - Tag
